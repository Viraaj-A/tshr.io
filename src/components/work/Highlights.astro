---
const { company, work, url, thumbnail } = Astro.props;
---

<a
  href={url}
  title={company}
  aria-label={company}>
  <div class="relative overflow-hidden rounded-2xl">
    <img
      class="aspect-[4/4] relative rounded-2xl object-top object-cover"
      src={thumbnail}
      alt={company}
    />
    <div
      class="p-4 w-full flex items-center justify-center  gap-2">
      <p class="text-green-900 font-semibold">{company}</p>
      —
      <p class="text-green-600 font-normal">{work}</p>
    </div>
  </div>
</a>
